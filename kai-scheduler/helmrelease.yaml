# Generated by AICR
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kai-scheduler
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: kai-scheduler
  install:
    createNamespace: true
  chart:
    spec:
      chart: kai-scheduler
      version: v0.14.1
      sourceRef:
        kind: HelmRepository
        name: ghcr-io-kai-scheduler-kai-scheduler
        namespace: flux-system
      interval: 5m
  dependsOn:
    - name: gpu-operator
  values:
    admission:
      gpuPodRuntimeClassName: ""
    global:
      tolerations:
      - operator: Exists
    postCleanup:
      enabled: false

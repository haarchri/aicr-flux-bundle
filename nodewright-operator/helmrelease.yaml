# Generated by AICR
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nodewright-operator
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: skyhook
  install:
    createNamespace: true
  chart:
    spec:
      chart: skyhook-operator
      version: 0.15.1
      sourceRef:
        kind: HelmRepository
        name: helm-ngc-nvidia-com-nvidia-skyhook
        namespace: flux-system
      interval: 5m
  dependsOn:
    - name: nfd
  values:
    controllerManager:
      manager:
        resources:
          limits:
            cpu: 1000m
            memory: 4000Mi
          requests:
            cpu: 1000m
            memory: 2000Mi
      tolerations:
      - operator: Exists
    fullnameOverride: skyhook-operator
    limitRange:
      default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: 500m
        memory: 512Mi

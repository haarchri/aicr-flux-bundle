# Generated by Cloud Native Stack
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nvsentinel
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: nvsentinel
  install:
    createNamespace: true
  chart:
    spec:
      chart: nvsentinel
      version: v1.3.0
      sourceRef:
        kind: HelmRepository
        name: ghcr-io-nvidia
        namespace: flux-system
      interval: 5m
  dependsOn:
    - name: nvidia-dra-driver-gpu
  values:
    fullnameOverride: nvsentinel
    global:
      systemNodeTolerations:
      - operator: Exists
      tolerations:
      - operator: Exists
    networkPolicy:
      enabled: false
    platformConnector:
      maxUnavailable: 1
      resources:
        limits:
          cpu: 200m
          memory: 512Mi
        requests:
          cpu: 200m
          memory: 512Mi
      updateStrategy: RollingUpdate
